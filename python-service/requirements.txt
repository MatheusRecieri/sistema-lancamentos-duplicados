# --- Servidor ---
fastapi
uvicorn[standard]
gunicorn
python-multipart

# --- Processamento de PDF / OCR ---
pdfplumber
pypdf
pymupdf               # (fitz) - leitura de layout e coordenadas
pytesseract           # OCR para PDFs escaneados
pdf2image             # converte PDF para imagem (usado com pytesseract)
pillow                # suporte a imagens (requisito do pytesseract/pdf2image)
poppler-utils         # dependência do pdf2image (instalado no sistema, não via pip)

# --- Processamento e análise de dados ---
pandas
openpyxl
xlrd
xlsxwriter

# --- Análise de texto e similaridade ---
rapidfuzz
python-Levenshtein

# --- Utilitários e Configuração ---
python-dotenv
